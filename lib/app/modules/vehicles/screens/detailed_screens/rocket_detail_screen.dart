import 'package:flutter/material.dart';

class RocketDetailScreen extends StatelessWidget {
  static const routeName = "/rocket-detail";

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("rocket detail screen"),
    );
  }
}
