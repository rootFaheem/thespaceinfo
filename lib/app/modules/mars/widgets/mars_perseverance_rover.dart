import 'package:flutter/material.dart';

class MarsPerseveranceRover extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Container(
        child: Text("Perseverance rover goes here..."),
      ),
    );
  }
}
