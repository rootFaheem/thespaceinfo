import 'package:flutter/material.dart';

class MarsDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mars detail goes here'),
    );
  }
}
