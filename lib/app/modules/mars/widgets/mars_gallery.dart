import 'package:flutter/material.dart';

class MarsGallery extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Container(
        child: Text("gallery goes here..."),
      ),
    );
  }
}
